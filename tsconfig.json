{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": ["src"],
  "exclude": [],
  "references": [{ "path": "./tsconfig.node.json" }],
  "compilerOptions": {
    "baseUrl": ".",
    "noEmit": true // Prevent emitting files during development - only tsconfig.build.json should emit
  }
}
